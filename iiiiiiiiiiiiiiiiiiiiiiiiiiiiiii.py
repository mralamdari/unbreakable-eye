do I need this ?
async def test_alert(request: Request):
async def test_report(request: Request, report_type: str = "daily"):

I mean I've used it for the test and it is just for the debuging purposes but it only returns me zero values, Why?




I need you explain these a little and I think this is a good topic for the techincal article:
    "# Three-phase pipeline restart engine"


explain this abit too:
    async def lifespan(app: FastAPI):

