# ── Production requirements ───────────────────────────────────────────────────
# Only packages your code actually imports (directly or as transitive deps).
# torch / torchvision / triton are NOT here — inference runs on ONNX Runtime.
# That alone removes ~5-8 GB from the Docker image.
#
# For GPU support, see requirements-gpu.txt
# For dev/test tools, see requirements-dev.txt
# For optional backends, see requirements-optional.txt

# Web framework
fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.26
itsdangerous==2.2.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
pydantic==2.12.5
pydantic-settings==2.12.0
python-dotenv==1.2.1

# Vision / Inference (CPU)
numpy>=1.26.4,<2.3
opencv-python-headless==4.12.0.88
onnxruntime==1.23.2
supervision==0.29.0
Pillow>=10.0.0

# Database
psycopg2-binary==2.9.9

# Model downloading
huggingface-hub==1.19.0
requests==2.32.5

# Logging / utilities
loguru==0.7.3
psutil==7.2.1
