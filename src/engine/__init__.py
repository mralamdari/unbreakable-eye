
# Add a rolling exemplar pool with a hard cap per customer.
# Add a quality gate before storing any embedding.
# Add track-confirmation hysteresis before relabeling a person.
# Only then tune detector confidence / NMS.